version: '3.8'
services:
  math-architect:
    build:
      context: .
      args:
        - API_KEY=${API_KEY}
    ports:
      - "8080:80"
    restart: always
    environment:
      - NODE_ENV=production
